services:
  - type: web
    name: bse-connector
    runtime: python
    buildCommand: pip install -e .
    startCommand: bse-connector-http
    envVars:
      - key: PORT
        value: "10000"
      - key: PYTHON_VERSION
        value: "3.11.11"
    healthCheckPath: /health
    plan: free
