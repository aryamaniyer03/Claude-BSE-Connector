services:
  - type: web
    name: bse-connector
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "10000"
      - key: BASE_URL
        value: "https://eqplus.ai"
    healthCheckPath: /health
    plan: free
